function pigLatin(word) {
let pigVal;
let wordToArray = word.split('');
  for(let i = 0; i <=vowel.length; i += 1) {
    if (wordToArray[0] === vowel[i]){
      let pigVal = word + "way";
    }
  }  
  let pigValToString = pigVal.toString();
  return pigValToString;
};

pigLatin(word);